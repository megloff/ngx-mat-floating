import { NgxMatFloatingTitleDirective } from './ngx-mat-floating-title.directive';

describe('NgxMatFloatingTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxMatFloatingTitleDirective();
    expect(directive).toBeTruthy();
  });
});
