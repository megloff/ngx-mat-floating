import {NgxMatFloatingDirective} from "./ngx-mat-floating.directive";

describe("NgxMatFloatingDirective", () => {
    it("should create an instance", () => {
        const directive = new NgxMatFloatingDirective();
        expect(directive).toBeTruthy();
    });
});
