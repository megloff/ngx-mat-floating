import { NgxMatFloatingContentDirective } from './ngx-mat-floating-content.directive';

describe('NgxMatFloatingContentDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxMatFloatingContentDirective();
    expect(directive).toBeTruthy();
  });
});
